# Slim dependency set for the STANDALONE FastAPI Ground Control service (api.py).
# Deliberately excludes streamlit / pydeck / pandas / osmnx so the Render build is
# fast and reliable. The model falls back to synthetic forest features when osmnx
# is absent, which is exactly the behaviour used in the live demo.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
